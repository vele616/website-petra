# Glossary

- **Consent checkbox**: Required opt-in checkbox used by contact and newsletter forms before submission.
- **Route-change reset**: A `useEffect` pattern keyed by `usePathname()` that clears transient form state when navigation occurs.
